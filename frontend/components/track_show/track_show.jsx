import React from 'react';
import { values } from 'lodash';

class TrackShow extends React.Component {
  constructor(props) {
    super(props);
  }
  // componentDidMount() {
  //   const trackId = Number(this.props.match.params.id);
  //   this.props.displayTrack(trackId);
  // }

  render() {
    return(
      <div>hi</div>
    )
  }
}

export default TrackShow;
